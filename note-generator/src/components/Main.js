import styled from "styled-components";

const MainCSS = styled.div`
  width: inherit;
  height: inherit;
`;

const Main = () => {
  return (
    <MainCSS>
      <p>Test</p>
    </MainCSS>
  );
};

export default Main;
